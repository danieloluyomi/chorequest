import {z} from 'zod';
export const choreSchema=z.object({title:z.string().trim().min(2).max(80),description:z.string().trim().max(500).default(''),category:z.enum(['bedroom','kitchen','school','pets','outdoors','family']),difficulty:z.enum(['easy','medium','hard']),points:z.number().int().min(1).max(1000),xp:z.number().int().min(1).max(1000),dueAt:z.string().datetime(),recurrence:z.enum(['none','daily','weekly']),requiresApproval:z.boolean(),requiresProof:z.boolean(),assigneeIds:z.array(z.string()).min(1)});
export const submissionSchema=z.object({note:z.string().trim().max(500).default(''),proofUrl:z.string().url().max(500).optional()});
export const decisionSchema=z.object({decision:z.enum(['approved','rejected','changes_requested']),note:z.string().trim().max(500).default('')}).superRefine((v,c)=>{if(v.decision!=='approved'&&!v.note)c.addIssue({code:'custom',message:'Feedback is required',path:['note']})});
export const rewardSchema=z.object({name:z.string().trim().min(2).max(80),description:z.string().trim().max(300).default(''),icon:z.string().max(8).default('🎁'),cost:z.number().int().min(1).max(100000),stock:z.number().int().min(0).max(10000),enabled:z.boolean(),requiresApproval:z.boolean()});
export const preferencesSchema=z.object({theme:z.enum(['garden','forest','midnight','ocean']),animation:z.boolean(),reducedMotion:z.boolean(),sound:z.boolean()});
